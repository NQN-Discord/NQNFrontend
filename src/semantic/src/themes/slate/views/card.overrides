
.ui.cards .ui.card, .ui.card {
  &:hover {
    background: lighten(@gray-darker, 15%);
  }

  .content>.header {
    color: @textColor;
  }

  &.inverted {
    &:hover {
      background: lighten(@primaryColor, 15%);
    }
  }
}